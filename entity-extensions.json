{
  "$schema": "https://www.jetbrains.com/help/youtrack/devportal/apps-entity-extensions.html",
  "comment": "App-owned custom properties (prefix scp). All app state is project-scoped versioned JSON so the backend (ctx.project / entities.Project) and the reminder workflow (issue.project) read and write the same documents. Native Sprint name/start/finish stay native and are managed through the current user's REST session, not here.",
  "entityTypeExtensions": [
    {
      "entityType": "Project",
      "properties": {
        "scpConfigJson": { "type": "string" },
        "scpSprintDataJson": { "type": "string" },
        "scpReminderStateJson": { "type": "string" }
      }
    },
    {
      "entityType": "User",
      "properties": {
        "scpPrefsJson": { "type": "string" }
      }
    }
  ]
}
